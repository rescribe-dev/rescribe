mutation register($username: String!, $name: String!, $email: String!, $password: String!, $recaptchaToken: String!) {
  register(username: $username, name: $name, email: $email, password: $password, recaptchaToken: $recaptchaToken)
}
