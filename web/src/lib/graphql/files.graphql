fragment fileDataFields on File {
  _id
  name
}

query files($repositories: [ObjectId!]!, $perpage: Int!, $page: Int!) {
  files(repositories: $repositories, perpage: $perpage, page: $page) {
    ...fileDataFields
  }
}