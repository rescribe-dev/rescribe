mutation purchase($product: String!, $interval: IntervalType!, $coupon: String, $paymentMethod: ObjectId, $address: ObjectId) {
  purchase(product: $product, interval: $interval, coupon: $coupon, paymentMethod: $paymentMethod, address: $address) {
    data
  }
}
