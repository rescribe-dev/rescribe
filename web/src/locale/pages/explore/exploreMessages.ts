// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface ExploreResultType {
    repoUrl: string;
    repoCode: string;
    repoName: string;
    repoLang: string;
    repoDes: string;
    selected: boolean;
};