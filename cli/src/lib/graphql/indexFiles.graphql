mutation indexFiles($saveContent: Boolean!, $files: [Upload!]!, $paths: [String!]!, $project: ObjectId!, $repository: ObjectId!, $branch: ObjectId!) {
  indexFiles(saveContent: $saveContent, files: $files, paths: $paths, project: $project, repository: $repository, branch: $branch)
}
