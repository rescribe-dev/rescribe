rescriber.py contains the class implementation of the rescriber. The rest of the files are either earlier implementations, equivalent implementations, or test files for those aforementioned implementations.

--PMARINA
