enum Plan {
  free
}

enum UserType {
  user
  admin
}

"""
User type
user account
"""
type User {
  _id: ID!
  name: String!
  email: String!
  "usage plan"
  plan: Plan!
  "user type"
  type: UserType!
}
