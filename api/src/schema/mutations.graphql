"""
Mutations
mutations for schema
"""
type Mutation {
  "register using email password"
  register(name: String!, email: String!, password: String!): String!
  "update user account"
  updateAccount(name: String, email: String, password: String): String!
  "delete account (with email for admins)"
  deleteAccount(email: String): String!
  indexGithub(paths: [String!]!, ref: String!, repositoryName: String!, repositoryOwner: String!, installationID: Int!): String!
  indexFiles(files: [Upload!]!, paths: [String!]!, repository: ID!, branch: ID!): String!
  deleteFile(id: ID!): String!
}
